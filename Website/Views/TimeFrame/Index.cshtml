@model IEnumerable<Admin.Models.TimeFrameDetailViewModel>
<h2>Time Frame List</h2>
<p>
    @Html.ActionLink("Create New", "Create", new { fromChild = ViewBag.FromChild })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FromHour)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ToHour)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FromHour)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ToHour)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id, fromChild = ViewBag.FromChild }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id, fromChild = ViewBag.FromChild }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id, fromChild = ViewBag.FromChild })
            </td>
        </tr>
    }
</table>
@if (ViewBag.FromChild && Model.Count() > 0)
{
    <script>
        $(document).ready(function () {
            window.opener.refreshTimeFrameList("@Newtonsoft.Json.JsonConvert.SerializeObject(Model)");
        });
    </script>
}
